{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with invalid source.sha256 \u2014 validator stops at sha check; despite filename, the data's name handling is not actually exercised (TODO follow-up)",
  "expected_error_contains": "source.sha256"
}
