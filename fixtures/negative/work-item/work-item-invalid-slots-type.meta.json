{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with slots field as JSON object instead of array \u2014 JSON parse rejects at unmarshal time",
  "expected_error_contains": "cannot unmarshal object into Go struct field IRDoc.slots of type []codegen.Slot"
}
