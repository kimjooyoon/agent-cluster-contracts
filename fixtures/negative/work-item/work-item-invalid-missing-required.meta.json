{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with source.dsl_file field missing \u2014 validator rejects with 'must match dsl/*.lisp' (empty string fails the regex)",
  "expected_error_contains": "source.dsl_file"
}
