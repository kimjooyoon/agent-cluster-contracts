{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with invalid sha AND missing slot.type \u2014 validator surfaces both errors; substring picks the missing-type check",
  "expected_error_contains": "slots[0].type: required"
}
