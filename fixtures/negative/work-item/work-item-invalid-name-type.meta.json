{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with name field as JSON number instead of string \u2014 JSON parse rejects at unmarshal time",
  "expected_error_contains": "cannot unmarshal number into Go struct field IRDoc.name of type string"
}
