{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with invalid sha AND empty slot.name \u2014 validator surfaces both errors; substring picks the kebab-case check",
  "expected_error_contains": "must be kebab-case"
}
