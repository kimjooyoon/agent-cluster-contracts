{
  "fixture_type": "ir-aggregate",
  "verifier": "ir-aggregate validate",
  "expected": "fail",
  "from_role": "dumb-agent",
  "supersedes": null,
  "purpose": "ir-aggregate with slot.required field as JSON string instead of bool \u2014 JSON parse rejects at unmarshal time",
  "expected_error_contains": "cannot unmarshal string into Go struct field Slot.slots.required of type bool"
}
